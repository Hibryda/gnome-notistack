{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug gnome-notistack (daemon)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/target/debug/gnome-notistack",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build",
      "comment": "Requires the CodeLLDB extension. Binary/path materialize once the daemon/ crate exists."
    }
  ]
}
